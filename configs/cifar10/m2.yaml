base_config: base.yaml
experiment:
  model_id: M2
model:
  variant: struct
  type: potential_unet
  base_channels: 80
train:
  amp: true
  grad_accum_steps: 4
  total_steps: 80000
compute:
  compute_limited: true
  tier: main_limited
loss:
  lambda_sym: 0.0
  mu_loop: 0.0
  mu1: 0.0
  mu2: 0.0
