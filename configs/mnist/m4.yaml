base_config: base.yaml
experiment:
  model_id: M4
model:
  variant: baseline
  type: unet
  hybrid:
    high_type: unet
    low_type: potential_unet
    low_base_channels: 56
loss:
  alpha: 0.05
  beta: 0.025
  sigma_c: 0.50
  mu2: 0.0
  boundary_subset: 8
  cycle_subset: 8
  cycle_samples: 8
  match_freq: 1
  cycle_freq: 2
  cycle_same_sigma: true
